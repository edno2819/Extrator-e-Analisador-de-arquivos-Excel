pytest


poetry new project_name / petry init (para projetos ja iniciados)
poetry install
poetry show --tree
poetry add libray_name / poetry add --dev libray_name

poetry shell - inicializa a venv 
deactivate -desativa

poetry run comand / poetry run pytest ou script.py

python -m venv venv
deactivate